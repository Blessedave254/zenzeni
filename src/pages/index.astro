---
import BaseLayout from '@layouts/BaseLayout.astro';
import HomePage from '@components/HomePage.astro';
import { type Locale } from '@lib/i18n';

const locale = 'en' as Locale;
---

<BaseLayout
  title="Kenya Safari Tours, Beach Holidays and Travel Packages"
  description="Book premium Kenya safaris, beach holidays, corporate excursions, hiking trips, and custom travel packages with Zenzeni Africa Safari."
  canonical="/"
>
  <HomePage locale={locale} />
</BaseLayout>
